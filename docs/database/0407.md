# 七、索引创建策略：三星索引法

三星索引法是针对单条 SELECT 语句，评价联合索引好坏的一套评分体系

## （一）第一星（过滤星）

等值条件放在前面

作用：尽量少扫描行，减少 IO

## （二）第二星（排序星）

索引字段顺序和 ORDER BY/GROUP BY 完全匹配，利用 B+ 树天然有序，避免 filesort 文件排序。

注意：范围条件后，索引不再支持排序。

## （三）第三星（覆盖星）

索引包含 SELECT 查询需要全部列，实现覆盖索引（Covering Index），完全不需要回表访问主表，执行计划 Extra 出现`Using index`
