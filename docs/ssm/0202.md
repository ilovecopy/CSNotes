# 二、DispatcherServlet 核心分发器

DispatcherServlet 是 SpringMVC 的核心前端控制器，它就像一个交通枢纽或者中央调度器，负责：

接收所有的 HTTP 请求

根据请求 URL 找到对应的 Controller 方法

调用 Controller 方法

处理响应并返回

## （一）RequestMappingHandlerMapping

RequestMappingHandlerMapping 是 SpringMVC 中专门负责请求 URL 和 Controller 方法之间的映射

根据 URL 找 Controller
