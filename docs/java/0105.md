# 五、包装类

一共有 8 种包装类。

把 8 种基本类型【包一层】变对象：为了能放进 ArrayList<Integer>这种泛型集合里（泛型擦除要求必须是 Object 子类）。

```java
Integer a = Integer.valueOf(127);
Integer b = 127;//自动装箱
Integer c = Integer.parseInt("127");
```

## （一）手动装箱

把 int 包装成 Integer

## （二）手动拆箱

把 Integer 变成 int

#
