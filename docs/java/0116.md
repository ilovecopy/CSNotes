# 十六、泛型：编译期类型检查、运行时类型擦除（为什么 List<String>和 List<Integer>是同一个 Class）

我要把员工按部门分组，没有泛型只能写 List list=new ArrayList();list.add(new Employee());Employee e=(Employee)list.get(0);每次都强转还可能转型错；有了泛型 List<Employee>编译器就保证里面只能塞 Employee，运行时不用强转。泛型的本质就是【编译期给编译器看的类型标签，运行时就擦没了】
